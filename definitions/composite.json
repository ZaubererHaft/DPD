{
	"patternName" : "Composite",
	"query" : "SELECT * FROM classifier c WHERE c.type IN ('ABSTRACT','DEFAULT') AND EXISTS (SELECT * FROM derivation d WHERE c.id = d.target_id AND EXISTS (SELECT * FROM association a WHERE a.source_id = d.source_id AND a.target_id = c.id));"
}