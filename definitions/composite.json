{
	"patternName" : "Composite",
	"query" : "SELECT c1 FROM Class c1 WHERE EXISTS (SELECT s FROM Subclass s WHERE c1 = s.target AND EXISTS (SELECT a FROM Association a WHERE a.source = s.source AND a.target = c1))"
}