{
	"patternName" : "Strategy",
	"query" : "SELECT * FROM classifier c1 WHERE c1.type IN ('ABSTRACT', 'DEFAULT') AND EXISTS (SELECT * FROM classifier c2 WHERE c2.type = 'INTERFACE' AND EXISTS (SELECT * FROM method m WHERE m.classifier_id = c1.id AND EXISTS (SELECT * FROM methodinvocation mi WHERE mi.classifier_id = c2.id AND mi.method_id = m.id)));"
}